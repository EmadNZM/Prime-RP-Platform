import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { copy,isLocale,Locale } from "@/lib/i18n"; import { db } from "@/lib/db"; import type { Rule } from "@prisma/client"; import { notFound } from "next/navigation"; export const dynamic="force-dynamic";
export default async function Rules({params}:{params:Promise<{locale:string}>}){const {locale:raw}=await params;if(!isLocale(raw))notFound();const locale=raw as Locale,t=copy[locale];let rules: Rule[]=[];try{rules=await db.rule.findMany({where:{published:true},orderBy:{sortOrder:"asc"}})}catch{}const fallback=[{id:"1",category:"GENERAL",titleAr:"احترام تجربة اللعب",titleEn:"Respect the roleplay",contentAr:"احترم اللاعبين والتزم بسياق الشخصية ولا تفسد تجربة الآخرين.",contentEn:"Respect other players, stay in character and avoid disrupting the experience."},{id:"2",category:"FAIR PLAY",titleAr:"اللعب النزيه",titleEn:"Fair play",contentAr:"يُمنع استغلال الأخطاء أو أي طريقة تمنح أفضلية غير عادلة.",contentEn:"Do not exploit bugs or use methods that create an unfair advantage."},{id:"3",category:"STAFF",titleAr:"قرارات الإدارة",titleEn:"Staff decisions",contentAr:"اتبع تعليمات الإدارة واستخدم نظام الدعم لحل النزاعات.",contentEn:"Follow staff instructions and use support channels to resolve disputes."}];const displayedRules=rules.length?rules:fallback;return <><SiteHeader locale={locale}/><main><section className="section" style={{paddingTop:160}}><div className="container"><div className="section-head"><div><span className="section-kicker">Prime RP</span><h2>{t.rules}</h2></div><p>{t.rulesText}</p></div><div className="rule-list">{displayedRules.map((r,i)=><article className="rule-row" key={r.id}><b>{String(i+1).padStart(2,"0")} · {r.category}</b><h3>{locale==='ar'?r.titleAr:r.titleEn}</h3><div style={{color:"#aaa",lineHeight:1.9}}>{locale==='ar'?r.contentAr:r.contentEn}</div></article>)}</div></div></section></main><SiteFooter locale={locale}/></>}
