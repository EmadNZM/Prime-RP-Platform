import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { copy,isLocale,Locale } from "@/lib/i18n"; import { db } from "@/lib/db"; import type { Job } from "@prisma/client"; import { notFound } from "next/navigation"; import { BriefcaseBusiness } from "lucide-react"; export const dynamic="force-dynamic";
export default async function Jobs({params}:{params:Promise<{locale:string}>}){const {locale:raw}=await params;if(!isLocale(raw))notFound();const locale=raw as Locale,t=copy[locale];let jobs: Job[]=[];try{jobs=await db.job.findMany({where:{active:true},orderBy:{sortOrder:"asc"}})}catch{}return <><SiteHeader locale={locale}/><main><section className="section" style={{paddingTop:160}}><div className="container"><div className="section-head"><div><span className="section-kicker">Careers</span><h2>{t.jobsTitle}</h2></div><p>{t.jobsText}</p></div><div className="cards">{jobs.map(j=><article className="card" key={j.id}><div className="icon"><BriefcaseBusiness size={19}/></div><h3>{locale==='ar'?j.titleAr:j.titleEn}</h3><p>{locale==='ar'?j.descriptionAr:j.descriptionEn}</p></article>)}</div></div></section></main><SiteFooter locale={locale}/></>}
